<p id="notice"><%= notice %></p>

<h1>Lunches</h1>

<table>
  <thead>
    <tr>
      <th> Lunch Type |  </th>
      <th> Rank |  </th>
      <th> User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lunches.each do |lunch| %>
      <tr>
        <td><%= lunch.food_type %></td>        
        <td><%= lunch.rank %></td><span> upvote</span> <span>downvote</span>
        

        
        <!-- under construction  -->
      

        <td><%= button_to '+1', {:controller => :lunches, :action => :upvote, :id => lunch}%></td>

        <td><%= button_to '-1', {:controller => :lunches, :action => :downvote, :id => lunch}%></td><br>
        <td> Will be email, evantually</td>
        
        <td><%= link_to 'Show |', lunch %></td>
        <td><%= link_to 'Edit |', edit_lunch_path(lunch) %></td>
        <td><%= link_to 'Destroy', lunch, method: :delete, data: { confirm: 'Are you sure?' } %></td>
         <!-- <%= link_to 'Destroy', {:controller=>:lunches,:action=>:destroy, :id=>lunch}, :method=>:delete %>-->
      </tr> 
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New lunch', new_lunch_path %>